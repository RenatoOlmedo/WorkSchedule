@page
@model WorkSchedule.Pages.Escala.listEscalaModel

@{
    ViewData["Title"] = "listEscala";
}

<h1>listEscala</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.escala[0].idPessoaEscala)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.escala[0].mes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.escala) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idPessoaEscala)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.mes)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
